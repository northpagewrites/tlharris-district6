{
  "name": "tl-harris-public-service-site",
  "version": "0.3.0",
  "private": true,
  "description": "Build-time validation tooling for the T. L. Harris public-service site. The site itself ships no JavaScript.",
  "scripts": {
    "validate": "bash scripts/validate.sh",
    "validate:blocks": "node tools/validate-blocks.mjs"
  },
  "devDependencies": {
    "@wordpress/block-library": "^11.0.0",
    "@wordpress/blocks": "^16.0.0",
    "jsdom": "^24.0.0"
  }
}
